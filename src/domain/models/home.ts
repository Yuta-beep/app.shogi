export type HomeSnapshot = {
  playerName: string;
  rating: number;
  pawnCurrency: number;
  goldCurrency: number;
  playerRank: number;
  playerExp: number;
  stamina: number;
  maxStamina: number;
  nextRecoveryAt: string | null;
};
